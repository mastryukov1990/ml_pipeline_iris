train:
  test_size: 0.9
  model: decisiontreetlassifier
  features: ['sepal length (cm)', 'sepal width (cm)', 'petal length (cm)', 'petal width (cm)']

eval:
  metrics:
  - precision
  - recall
  - accuracy
